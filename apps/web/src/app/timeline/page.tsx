import { TimelineWorkspace } from "@/components/timeline-workspace";

export const metadata = {
  title: "Historical Intelligence Timeline | DORA",
  description:
    "Synchronized market, risk, manufacturing, forecast and recommendation history.",
};

export default function TimelinePage() {
  return <TimelineWorkspace />;
}
